name: Lab 4 Helm Chart Check

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  helm-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Start Minikube
        uses: medyagh/setup-minikube@latest

      - name: Run Master Validation Script
        run: |
          chmod +x ./tests/lab4_test.sh
          ./tests/lab4_test.sh